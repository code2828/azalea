(
{++} (=_1,+@_1,1),
{--} (=_1,-@_1,1),
{for} ( _1, :_, ?_2, (_4, _3, ->_), $$_ )
{while} ( :_, ?_1, (_2, ->_), $$_ )

#p, 26;
#i;
#number;
#counter;
#remainder;

=p, 2;

for (=number,3;=counter,1), (<@number;100), (++number,++counter),
(
    :continue,
    for (=i,0), (&<@i;@counter;<*@p,i,@p,i;@number), (++i),
    (
        (=remainder, %@number; @p, i),
        ?==@remainder; 0,
        (
            ++number,
            ->continue
        );
    ),
    =$(+p, @counter), @number;
),

$$p, i, number, counter;
0
)
